# Keep text files as LF so GitHub Actions and Linux Docker checkouts
# stay consistent on Windows (overrides core.autocrlf).
* text=auto eol=lf

Dockerfile text eol=lf
*.yml text eol=lf
*.yaml text eol=lf
*.md text eol=lf